<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartHomeDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/light_purple</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="cardBackgroundColor">@color/card_background_dark</item>
        <!-- 设置夜间模式文本颜色 -->
        <item name="android:textColorPrimary">@color/text_color_dark</item>
        <item name="android:textColor">@color/text_color_dark</item>
        <item name="android:textColorSecondary">@color/text_color_dark</item>
        <item name="android:textColorTertiary">@color/text_color_dark</item>
    </style>
</resources>