<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="RoundedImage">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>   <!-- 4个角统一 -->
        <!-- 也可以单独指定：topLeft、topRight、bottomLeft、bottomRight -->
    </style>

    <!-- 统一按钮样式 -->
    <style name="AppTheme.Button" parent="Widget.Material3.Button">        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>

    <!-- 小按钮样式 -->
    <style name="AppTheme.Button.Small">
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">6dp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>
</resources>